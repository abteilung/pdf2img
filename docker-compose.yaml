version: '3'

services:
    web:
        build: .
        ports:
            - '5000:5000'
        volumes:
            - ./app:/app
            - ./uploads:/app/uploads
        depends_on:
            - redis
        environment:
            - FLASK_ENV=production

    redis:
        image: 'redis:alpine'
        ports:
            - '6179:6379'
